#include <iostream>
#include <cstdio>

using namespace std;

int main()
{
    int t;
    int n, N;
    
    while(scanf("%d", &t) == 1) {
        for(int i = 1; i <= t; i++) {
            int sum = 0;
            
            scanf("%d", &n);
            int iterartor = (2 * n) - 1;
            
        }
    }
    
    
    return 0;
}